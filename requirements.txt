# NOTE: intentionally includes older dependencies for dependency-scanning tests.
# Updated to fix known CVEs:
# - CVE-2023-30861: Flask session cookie leak (CVSS 7.5) - upgraded to >= 2.3.2
# - CVE-2018-18074: Requests credential leak (CVSS 7.5) - upgraded to >= 2.20.0
# - CVE-2020-14343: PyYAML RCE (CVSS 9.8) - upgraded to >= 5.4
# - CVE-2017-18342: PyYAML arbitrary code execution (CVSS 9.8) - upgraded to >= 5.1
Flask==3.0.0
requests==2.31.0
PyYAML==6.0.1
boto3==1.34.0
