# NOTE: contains intentionally older dependencies for dependency-scanning tests.
# Security fixes applied: upgraded vulnerable packages
Flask==3.0.0       # CVE-2023-30861: session cookie caching fix (was 1.0.2)
requests==2.32.3   # CVE-2018-18074: credential exposure on redirect fix (was 2.18.4)
PyYAML==6.0.1      # CVE-2017-18342, CVE-2020-14343: arbitrary code execution fix (was 3.13)
boto3==1.7.0       # older boto3 version (no critical CVEs)
