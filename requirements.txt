# NOTE: intentionally includes older dependencies for dependency-scanning tests.
Flask==3.0.0       # upgraded from 1.0.2 - fixes CVE-2023-30861 (session cookie caching)
requests==2.32.3   # upgraded from 2.18.4 - fixes CVE-2018-18074 (credential exposure on redirect)
PyYAML==6.0.1      # upgraded from 3.13 - fixes CVE-2017-18342 and CVE-2020-14343 (arbitrary code execution)
boto3==1.7.0       # older boto3 version
